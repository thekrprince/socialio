
const CreatePostModal = ({ setIsOpen }) => {
    return (
        <>
        <div className="" onClick={() => setIsOpen(false)} />
        <h1 >
            create Post
        </h1>
        <input type="textarea" />
        </>
    );
};

export default CreatePostModal;